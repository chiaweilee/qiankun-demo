{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run dev --parallel --no-cache"
  },
  "devDependencies": {
    "turbo": "latest"
  }
}
